# Maven
target/
*.class

# NetBeans
nbproject/private/
nbproject/Makefile-*.mk
nbproject/Package-*.bash
build/
nbbuild/
dist/
nbdist/
.nb-gradle/

# Fitxers temporals / Temporary files
*.log
*.tmp
*.bak
*.swp

# Portable build (gran i generat) / Portable build (large and generated)
portable/
portable_bkp/
_build_jre/

# JRE integrat al projecte (binaris, no al repositori) / Embedded JRE (binaries, not in repo)
vendor/

# ZIP de distribució / Distribution ZIP
*.zip

# Exportacions puntuals / One-off exports
export.svg
prova.mid

# IDE genèrics / Generic IDE
.idea/
*.iml
.vscode/

# Claude Code (sessions, worktrees) / Claude Code (sessions, worktrees)
.claude/

# Configuració local generada (rutes de màquina) / Local generated config (machine paths)
config/

# Material personal de treball, no part del projecte públic / Personal working material, not part of the public project
Dodecagrams/
OtherSongs/
Prompts/
SongsInBooklet - bkp/
messages_ca_original_temp.properties
